// Provides the build-time type for the legacy MoonSphere material helper.
// The helper is not invoked at runtime, but TypeScript still validates the
// constant reference while compiling the component.
declare const MOON_EMISSIVE_INTENSITY: number;
